Ext.define('Aenis.store.main.contact.Juridical', {
    extend: 'Ext.data.Store',
    model: 'Aenis.model.main.contact.Juridical',

    autoLoad: false,
    autoDestroy: true,
    autoSync: true,
    pageSize: 10

});
